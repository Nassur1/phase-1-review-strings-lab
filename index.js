// Write your code in this file!
//Define the current user as a non-empty string
const currentUser = 'Grace Hopper';

//Create a welcome message using currentUser
const welcomeMessage = 'Welcome to Flatbook, ${Grace Hopper}!';

//Create an excited version of the welcome message in all caps
const excitedWelcomeMessage = 'WELCOME TO FLATBOOK, ${GRACE HOPPER.toUpperCase()}!';

//Create a short greeting using only the first initial of currentUser
const shortGreeting = 'Welcome, ${G.slice(0,1)}!';
